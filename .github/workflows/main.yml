name: main

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: project setup
        run: make setup

      - name: run tests
        run: make test

      - name: run lint
        run: make lint

